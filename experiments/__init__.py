"""Standalone studies that build on the core library in ``src`` and the
experiment harness in ``configs``. Each module is runnable from the repository
root, e.g. ``python -m experiments.pendulum_convergence``.
"""
